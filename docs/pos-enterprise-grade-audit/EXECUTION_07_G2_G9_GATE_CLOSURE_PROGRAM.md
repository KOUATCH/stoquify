# Stoquify G2–G9 systematic gate-closure program

Status: **BLOCKED AT G1; LATER-GATE PREFLIGHT ALLOWED**  
Active gate: **G1**  
Candidate: **development rolling, dirty, not promotable**

## Program rule

The gates are dependency contracts, not a list that can be checked in parallel and declared complete. A later gate may be inspected and prepared, but it cannot pass while a required earlier gate remains blocked. Local static/unit evidence is never promoted into provider, statutory, physical-device, production-operations or pilot proof.

## Ordered closure waves

| Wave | Gates | Outcome required before advancing |
| --- | --- | --- |
| W0 | G1 | D-01 through D-11 receive role-complete, fresh-authenticated, artifact-bound approvals |
| W1 | G2 | Cart, commit, stock and access invariants pass on real PostgreSQL with synthetic fixtures |
| W2 | G3A + G3B | Provider sandbox money truth and immutable country-controlled document truth pass independently |
| W3 | G4 + G6 | Named workstation/device scope and physical fulfillment truth pass |
| W4 | G5 + G7 | Store close and delivery-based financial O2C tie out |
| W5 | G8 | Production-like performance, recovery, security, privacy, observability and incident evidence passes |
| W6 | G9 | R0–R6 cohorts expand only through signed decisions and automatic stop conditions |

## Current gate matrix

| Gate | Current state | Main closure boundary |
| --- | --- | --- |
| G1 | **Blocked** | 13/13 technical checks; 0/11 authentic decision approvals |
| G2 | **Blocked / partial** | A01–A04 complete; cart identity/version/stale-write, availability, full PostgreSQL races and unified access/privacy matrix remain |
| G3A | **Blocked** | No named provider/merchant sandbox; canonical provider lifecycle and complete payment/accounting cases remain |
| G3B | **Blocked** | Fiscal/receipt foundations exist; immutable in-transaction source, governed numbering, durable worker/delivery recovery and qualified review remain |
| G4 | **Blocked** | Synthetic Edge EN/FR exists; complete workstation UX/accessibility and physical device evidence do not |
| G5 | **Blocked** | Shift/close/reconciliation foundations exist; governed BusinessDay, blind declaration, X/Z, statement posting and full tie-out remain |
| G6 | **Blocked** | Stock kernel exists; order reservation/fulfillment/backorder/cancellation aggregates and concurrency proof remain |
| G7 | **Blocked** | AR/statement foundations exist; delivered-quantity invoicing, return disposition and cross-domain correction proof remain |
| G8 | **Blocked** | Many static gates exist; clean candidate, production-like load/recovery/failover/rotation/security/alert/incident evidence remain |
| G9 | **Not started** | Requires G8 plus design-partner and cohort evidence with signed expansion decisions |

## G2 immediate technical repair order

Once G1 is authentically approved, execute G2 in this order:

1. Measure draft-cart and cart-line duplicates without changing data.
2. Add the one-active-draft identity and unique cart-line identity through an additive migration.
3. Add cart version and command ID to create/add/update/remove commands.
4. Return the authoritative cart with typed stale/conflict results.
5. Remove the current silent quantity clamp; return sellable-availability conflict and refreshed version.
6. Run PostgreSQL duplicate commit, duplicate scan, concurrent edit, stock issue/reservation, rollback, response-loss and replay races.
7. Introduce one `POSSurfaceAccess` decision for all navigation, route, read, command, offline, receipt, report, export and API surfaces.
8. Complete cashier allowlist, tenant/location/terminal negative tests, maker-checker, privacy DTO, receipt-secret, API permission, abuse, retention and redaction evidence.

## External-return boundaries

The following cannot be manufactured from repository templates:

- Accountable G1 decisions and signatures.
- A named payment provider, real merchant sandbox and official callback/statement specification.
- Qualified Cameroon fiscal/country review and independent checker verification.
- Physical device execution or an explicitly signed production exclusion.
- Production-like close, load, recovery, failover, rotation, alerting and incident drills.
- Design-partner participation and cohort expansion decisions.

## Validation policy

Every gate packet must bind a commit/configuration, list verification commands, distinguish local from external evidence, hash its artifacts, identify owners, state limitations and fail closed on missing evidence. Corrections create linked compensating facts. Completed sales, payments, movements, receipts, fiscal documents, journals and audit records are never rewritten or deleted.

The machine-readable source of truth is `EXECUTION_07_G2_G9_GATE_CLOSURE_PROGRAM.json`. The executable aggregate gate is `scripts/pos-enterprise-program-gate.js`.
