interface:
  display_name: "Module Release Gates Router"
  short_description: "Route legacy module release-gate requests"
  default_prompt: "Use $aqstoqflow-module-release-gates to delegate this legacy gate request to the canonical release and rollback skill."
