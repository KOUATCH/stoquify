interface:
  display_name: "Module Security Prerequisites"
  short_description: "Close module control-plane security blockers"
  default_prompt: "Use $aqstoqflow-module-security-prerequisites to close and verify module-system security prerequisites before enforcement."
