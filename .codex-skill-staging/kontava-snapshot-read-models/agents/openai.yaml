interface:
  display_name: "Kontava Snapshot Read Models"
  short_description: "Create fast evidence-graded snapshots for cross-module command centers."
  default_prompt: "Use this skill to design and implement tenant, branch, payment truth, inventory cash, close readiness, and rebuild snapshot foundations with evidence grades, freshness, blockers, redactions, tenant isolation, idempotency, and stale fallback behavior."
