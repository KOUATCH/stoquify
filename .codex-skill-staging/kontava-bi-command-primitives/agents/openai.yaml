interface:
  display_name: "Kontava BI Command Primitives"
  short_description: "Build shared BI command UI primitives"
  default_prompt: "Use $kontava-bi-command-primitives to build shared BI command dashboard components safely."

policy:
  allow_implicit_invocation: true
