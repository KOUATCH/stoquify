interface:
  display_name: "Kontava BI Command Foundation"
  short_description: "Establish shared BI command contracts"
  default_prompt: "Use $kontava-bi-command-foundation to establish shared BI command contracts before dashboard redesign."

policy:
  allow_implicit_invocation: true
