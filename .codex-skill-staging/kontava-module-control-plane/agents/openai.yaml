interface:
  display_name: "Kontava Module Control Plane"
  short_description: "Build module catalog, entitlements, observe mode, guards, and upgrade surfaces."
  default_prompt: "Use this skill to implement Kontava module catalog, tenant entitlements, plan dependencies, observe-mode would-block logging, server-side guards for routes/actions/APIs/reports/jobs, owner upgrade surfaces, and tests proving RBAC and wildcard permissions do not bypass subscription rules."
