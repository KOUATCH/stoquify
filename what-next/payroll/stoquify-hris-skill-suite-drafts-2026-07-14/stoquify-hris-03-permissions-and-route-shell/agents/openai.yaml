interface:
  display_name: "HRIS 03 Permissions And Route Shell"
  short_description: "Create the access and route shell that lets HRIS become visib..."
  default_prompt: "Use $stoquify-hris-03-permissions-and-route-shell to execute the Stoquify HRIS People Core slice in dependency order."
policy:
  allow_implicit_invocation: true
