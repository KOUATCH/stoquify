# Verification

## Focused Checks

```powershell
npm run typecheck
```

Use route-level smoke checks, screenshots, or accessibility checks when visual behavior changes. Run broader build checks only when shared shell, layout, or primitives change.
