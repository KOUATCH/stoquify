interface:
  display_name: "AqStoqFlow Package Read Model"
  short_description: "Builds service-owned package read models"
  default_prompt: "Use $aqstoqflow-module-package-read-model to build service-owned module package and entitlement read models."
