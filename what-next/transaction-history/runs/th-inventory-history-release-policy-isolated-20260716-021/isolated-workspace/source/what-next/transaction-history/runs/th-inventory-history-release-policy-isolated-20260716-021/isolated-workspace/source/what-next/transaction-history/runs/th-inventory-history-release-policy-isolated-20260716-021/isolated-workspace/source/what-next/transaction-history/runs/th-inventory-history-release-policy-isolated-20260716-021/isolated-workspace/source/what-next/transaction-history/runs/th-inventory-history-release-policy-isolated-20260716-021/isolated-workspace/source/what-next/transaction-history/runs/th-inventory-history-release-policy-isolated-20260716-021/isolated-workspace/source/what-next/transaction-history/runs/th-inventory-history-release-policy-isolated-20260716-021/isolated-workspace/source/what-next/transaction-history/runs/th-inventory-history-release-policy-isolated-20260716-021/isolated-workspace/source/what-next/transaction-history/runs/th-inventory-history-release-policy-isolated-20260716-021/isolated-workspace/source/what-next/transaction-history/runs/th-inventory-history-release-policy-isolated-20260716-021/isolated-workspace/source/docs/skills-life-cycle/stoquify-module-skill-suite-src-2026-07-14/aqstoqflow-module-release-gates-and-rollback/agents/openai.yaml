interface:
  display_name: "Module Release Gates And Rollback"
  short_description: "Certify module gates, evidence, and rollback"
  default_prompt: "Use $aqstoqflow-module-release-gates-and-rollback to evaluate the current module stage and produce a release and rollback decision."
