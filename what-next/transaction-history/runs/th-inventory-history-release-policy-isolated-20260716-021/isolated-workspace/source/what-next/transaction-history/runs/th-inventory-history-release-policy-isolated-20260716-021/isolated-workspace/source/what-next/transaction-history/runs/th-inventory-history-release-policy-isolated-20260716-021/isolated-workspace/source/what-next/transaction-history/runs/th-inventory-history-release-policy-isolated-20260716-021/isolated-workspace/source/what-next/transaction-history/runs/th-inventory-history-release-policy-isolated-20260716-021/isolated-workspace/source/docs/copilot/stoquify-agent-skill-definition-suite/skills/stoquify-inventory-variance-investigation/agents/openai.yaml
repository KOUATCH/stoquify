
interface:
  display_name: "Inventory Variance Investigation"
  short_description: "Inventory Variance Investigation with safe Stoquify controls"
  default_prompt: "Use $stoquify-inventory-variance-investigation to explain negative stock, count variance, transfer conflict, replay conflict, and projection drift from service-owned event history."
