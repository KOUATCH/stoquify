
interface:
  display_name: "Agent Evidence Recorder"
  short_description: "Agent Evidence Recorder with safe Stoquify controls"
  default_prompt: "Use $stoquify-agent-evidence-recorder to persist version references, evidence fingerprints, policy decisions, approvals, tool outcomes, cost, and terminal status without copying unnecessary sensitive records."
