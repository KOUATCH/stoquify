
interface:
  display_name: "Agent Run State Machine"
  short_description: "Agent Run State Machine with safe Stoquify controls"
  default_prompt: "Use $stoquify-agent-run-state-machine to manage queued, running, waiting-for-approval, blocked, retryable, failed, cancelled, degraded, denied, and completed states with resumability and dependency-aware terminal rules."
