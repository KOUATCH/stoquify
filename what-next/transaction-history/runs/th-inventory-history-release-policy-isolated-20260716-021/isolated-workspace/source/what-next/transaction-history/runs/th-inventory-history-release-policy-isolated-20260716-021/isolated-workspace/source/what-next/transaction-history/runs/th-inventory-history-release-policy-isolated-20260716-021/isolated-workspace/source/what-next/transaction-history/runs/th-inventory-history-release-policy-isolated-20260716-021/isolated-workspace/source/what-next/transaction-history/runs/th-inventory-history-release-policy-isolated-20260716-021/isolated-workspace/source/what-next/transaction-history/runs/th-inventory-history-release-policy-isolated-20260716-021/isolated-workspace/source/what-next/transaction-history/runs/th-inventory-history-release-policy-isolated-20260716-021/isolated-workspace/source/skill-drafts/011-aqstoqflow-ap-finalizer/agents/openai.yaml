interface:
  display_name: "AqStoqFlow AP Finalizer"
  short_description: "Close remaining AP gates for 011"
  default_prompt: "Use $011-aqstoqflow-ap-finalizer to close AqStoqFlow 011 AP gates with UI, postings, reconciliation, country packs, RBAC, and verification."
policy:
  allow_implicit_invocation: true
