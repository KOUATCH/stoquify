interface:
  display_name: "Module Enforcement Truth"
  short_description: "Reconcile effective module enforcement state"
  default_prompt: "Use $aqstoqflow-module-enforcement-truth to reconcile observe, pilot, and enforced module behavior with operator-visible truth."
