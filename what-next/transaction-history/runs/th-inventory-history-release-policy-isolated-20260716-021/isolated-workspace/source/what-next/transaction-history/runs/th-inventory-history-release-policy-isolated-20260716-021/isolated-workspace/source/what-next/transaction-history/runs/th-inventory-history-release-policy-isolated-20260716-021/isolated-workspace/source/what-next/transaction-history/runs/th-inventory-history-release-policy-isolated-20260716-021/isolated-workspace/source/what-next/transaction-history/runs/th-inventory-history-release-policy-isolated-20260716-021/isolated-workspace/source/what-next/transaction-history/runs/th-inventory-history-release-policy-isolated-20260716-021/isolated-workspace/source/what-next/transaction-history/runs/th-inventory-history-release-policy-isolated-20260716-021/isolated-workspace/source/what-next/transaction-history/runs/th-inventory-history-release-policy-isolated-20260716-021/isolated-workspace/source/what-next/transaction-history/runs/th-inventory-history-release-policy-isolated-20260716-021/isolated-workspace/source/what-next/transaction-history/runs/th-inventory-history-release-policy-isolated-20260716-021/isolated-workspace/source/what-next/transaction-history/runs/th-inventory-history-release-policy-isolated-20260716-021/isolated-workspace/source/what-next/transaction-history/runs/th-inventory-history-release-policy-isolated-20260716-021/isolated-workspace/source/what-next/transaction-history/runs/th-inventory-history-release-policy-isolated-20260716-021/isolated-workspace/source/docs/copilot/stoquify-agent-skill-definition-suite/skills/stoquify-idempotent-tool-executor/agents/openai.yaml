
interface:
  display_name: "Idempotent Tool Executor"
  short_description: "Idempotent Tool Executor with safe Stoquify controls"
  default_prompt: "Use $stoquify-idempotent-tool-executor to invoke allowlisted deterministic service commands with validated schemas, actor context, idempotency keys, timeouts, and typed error mapping."
