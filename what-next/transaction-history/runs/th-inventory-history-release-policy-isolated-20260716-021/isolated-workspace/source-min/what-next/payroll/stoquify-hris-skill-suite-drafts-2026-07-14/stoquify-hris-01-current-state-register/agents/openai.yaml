interface:
  display_name: "HRIS 01 Current State Register"
  short_description: "Create the canonical current-state register for HRIS, payroll..."
  default_prompt: "Use $stoquify-hris-01-current-state-register to execute the Stoquify HRIS People Core slice in dependency order."
policy:
  allow_implicit_invocation: true
